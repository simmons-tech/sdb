import React, { useState } from "react";
import BasePage from "../BasePage";

const GuestListHistory = (props) => {
  const [loading, setLoading] = useState(true);

  return <BasePage loading={loading}>
    <p>Under construction</p>
  </BasePage>;
};
